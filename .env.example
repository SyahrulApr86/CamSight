# Port configuration (internal container ports)
BACKEND_PORT=8000
FRONTEND_PORT=3000

# External port for nginx reverse proxy
EXPOSED_PORT=80

# Backend URL for development (leave empty for production with nginx)
# Set to full URL only if not using nginx reverse proxy
NEXT_PUBLIC_BACKEND_URL=

# Internal backend host for Docker network
BACKEND_HOST=backend